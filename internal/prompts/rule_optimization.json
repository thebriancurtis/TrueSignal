{
  "name": "rule_optimization.json",
  "description": "Improves rule quality by maximizing clarity, conciseness, and alignment with core principles.",
  "instructions": "Execute rule_optimization.json based on the declared schema and standards.",
  "uses_standards": [
    "rule.json"
  ],
  "schema": {
    "inputs": {
      "type": "object",
      "properties": {
        "text": {
          "type": "string"
        }
      },
      "required": [
        "text"
      ]
    },
    "outputs": {
      "type": "object",
      "properties": {
        "optimized_text": {
          "type": "string"
        },
        "reasoning": {
          "type": "string"
        }
      },
      "required": [
        "optimized_text",
        "reasoning"
      ]
    }
  },
  "example_input": {
    "text": "Every API should require some kind of login before anything happens."
  },
  "example_output": {
    "optimized_text": "All APIs must require authentication.",
    "reasoning": "Simplified language while preserving intent and removing ambiguity."
  }
}