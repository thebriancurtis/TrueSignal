{
  "name": "validate_rule_file",
  "description": "Validates the internal structure of a rule file using the rule.json standard.",
  "instructions": "Ensure that the rule file conforms to the rule.json standard. This includes checking for required fields, valid data types, and structural completeness.",
  "uses_standard": "rule.json",
  "output_schema": {
    "type": "object",
    "properties": {
      "valid": {
        "type": "boolean"
      },
      "errors": {
        "type": "array",
        "items": {
          "type": "string"
        }
      }
    },
    "required": [
      "valid",
      "errors"
    ]
  },
  "example_input": {
    "rule": {
      "rule": "Avoid redundant expressions that add no meaning.",
      "category": "LNG"
    }
  },
  "example_output": {
    "valid": true,
    "errors": []
  }
}