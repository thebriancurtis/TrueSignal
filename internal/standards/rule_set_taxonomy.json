{
  "name": "rule_set_taxonomy.json",
  "description": "Defines the classification structure used to describe and categorize rule sets within the TrueSignal project.",
  "version": "1.0.0",
  "schema": {
    "type": "object",
    "properties": {
      "tags": {
        "type": "array",
        "items": {
          "type": "string"
        },
        "description": "A list of keywords or labels to support filtering and discoverability."
      },
      "description": {
        "type": "string",
        "description": "A human-readable explanation of the rule set\u2019s purpose and scope."
      }
    },
    "required": [
      "description"
    ]
  },
  "example": {
    "tags": [
      "login",
      "security",
      "access control"
    ],
    "description": "A foundational set of authentication rules intended for enforcement in production environments."
  }
}