# Rules

This folder contains atomic, validated rules that define specific assistant behaviors.

Each rule is a `.json` file and must pass:
- `rule.json` schema validation
- Eligibility standards
- Deduplication and optimization
